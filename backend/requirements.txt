fastapi>=0.110.0
uvicorn[standard]>=0.29.0
scikit-learn>=1.4.0          # 1.9.0 is pre-installed on Python 3.14
pandas>=2.0.0
joblib>=1.3.0
python-multipart>=0.0.9
pydantic>=2.6.0
firebase-admin>=7.0.0

# NOTE: On Python 3.14, some older pinned versions lack pre-built wheels.
# This requirements.txt uses >= ranges so pip uses whatever is already installed.
# If you are on Python <=3.12, tighter pins like scikit-learn==1.5.1 will work.
